# Frontier global configuration. Module gameplay settings live in modules/*.yml.
config-version: 1

database:
  jdbc-url: jdbc:postgresql://localhost:5432/frontier
  username: frontier
  password: frontier
  maximum-pool-size: 10

runtime:
  async-threads: 4

security:
  command-rate-limit: 12
  command-rate-window-seconds: 2
